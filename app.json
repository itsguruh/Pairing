const express = require("express");
const app = express();

app.get("/", (req, res) => {
  res.send(`
    <h1>CRYPTIX-MD Session ID Generator ✅</h1>
    <p>Your server is running on Heroku!</p>
    <p>Now you can plug in your Session ID generator logic here.</p>
  `);
});

const PORT = process.env.PORT || 3000;
app.listen(PORT, () => {
  console.log(`Server running on port ${PORT}`);
});
